apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-infra.fullname" . }}
  labels:
    {{- include "my-infra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-infra.selectorLabels" . | nindent 4 }}
